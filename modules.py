"""
As our code grows larger it is often useful to logically group
code into separate files. Python packages and modules are exactly the mechanism
to do that.
"""

#Let's build a function for Rrock Paper Scisors game
# that produces PC move.

# Let's add entry point __main__

# Create a separate module 

# Create a separate pacakge with new module